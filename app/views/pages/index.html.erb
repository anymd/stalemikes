


<%= render 'shared/notice' %>

<div class="alert alert-info"><i class="icon-info-sign"></i>
Stale Mikes requires comedy bookers to regularly verify their show is still alive and well.  Otherwise, it fades away!</div>
<br>

<%= render 'pages/filter' %>

<% if params[:view] == 'grid' %>
  <div id="show-box">
   <%= render 'shared/shows' %>
  </div>
<% else %>
  <%= render 'shared/show_list' %>
<% end %>

